'use client'

function Document({
    id, name, size, downloadUrl
}: {
    id: string;
    name: string;
    size: string;
    downloadUrl: string;
}) {
    const router = us
  return (
    <div>Document</div>
  )
}

export default Document